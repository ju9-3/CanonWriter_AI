# CanonWriter AI小说创作工具
AI小说生成项目，Python后端 + 静态HTML前端

## 项目结构
- backend：Python后端服务
- frontend：前端页面

## 后端启动
1. 创建虚拟环境
2. 激活虚拟环境，安装依赖
3. 复制 `.env.example` 重命名为 `.env`，填入你的密钥
4. 运行 `run_api.bat` 启动后端接口

## 前端启动
直接打开html文件，或者双击start.bat
